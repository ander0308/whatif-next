import { styled } from '../../styles'

export const StyleButton = styled("button", {
  backgroundColor: "$red900",
  border: "none",
  width: 300,
  height: "3rem",
  fontSize: 16,
  color: "$gray200",
  borderRadius: 4
})
